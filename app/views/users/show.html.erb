<%= stylesheet_link_tag params[:users_controller] %>

<% provide(:title, @user.username) %>

<%= image_tag(@user.gravatar) %>

<%= @user.email %>
<%= render 'shared/stats' %>
<%= render 'shared/user_info' %>
<%= render 'follow_form' if user_signed_in? %>
