
<div class="logo"> </div>


<div class="maincontent">

  <h1> <%= CONFIG[:app_name] %></h1>

  <p>
    Hello, and wellcome to <%= CONFIG[:app_name] %>
  </p>
  
    <%= if current_user then render 'poll/poll' unless Poll::Poll.first.blank? end %> <!-- This is here for testing -->


    <!-- You can do this for Stores::EntertainmentCategory and Stores::OtherCategory aswell 
    <ul>
      <% Stores::FoodCategory.all.each do |category| %> 
        <li><%= category.title %></li>
      <% end %>
    </ul>
    -->


</div>
