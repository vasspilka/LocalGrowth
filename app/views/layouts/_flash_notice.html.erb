<% flash.each do |name, msg| %>
  <div data-alert class="alert-box round radius <%= name == :notice ? "success" : "error" %>">
    <%= msg %>
    <a href="#" class="close">&times;</a>
  </div>
<% end %> 